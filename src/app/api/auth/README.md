// Aquest fitxer redirigeix al handler de NextAuth.
// La carpeta real hauria de ser [...]nextauth] però la creem aquí com a referència.
// Assegura't de crear la carpeta src/app/api/auth/[...nextauth]/route.ts manualment
// amb aquest contingut:
//
// import { handlers } from "@/lib/auth";
// export const { GET, POST } = handlers;

export {};
